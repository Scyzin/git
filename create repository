git bash运行
$ mkdir learngit//创建
$ cd learngit//切换目录
$ pwd //显示版本库所在目录
/Users/michael/learngit
$ git init//把目录变成git可以管理的仓库看
$ git add  readme.txt//把文件提交到仓库
$ git commit -m "wrote a readme file"//告诉gig，把文件提交到仓库

初始化一个Git仓库，使用git init命令。
添加文件到Git仓库，分两步：
第一步，使用命令git add <file>，注意，可反复多次使用，添加多个文件；
        git add命令实际上就是把要提交的所有修改放到暂存区（Stage）
第二步，使用命令git commit，完成。
        git commit就可以一次性把暂存区的所有修改提交到分支

